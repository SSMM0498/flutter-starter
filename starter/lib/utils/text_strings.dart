class TextStrings {
  // -- App Logos
  static const String appName = "starter";
}

class LoaderText {
  String title;
  String message;

  LoaderText({
    required this.title,
    required this.message,
  });
}